NOTICEBOARD GENERATOR - TO DO
==============================

These are content and data tasks still outstanding in the project.


CONTENT - TAG TABLES (src/legitimateContracts.js)
--------------------------------------------------

Contract table
  Where: src/legitimateContracts.js, the `contract` array
  Notes: Add more contract types. Each entry uses:
           { text: "do a thing", baseRewardGp: 50, weight: 10 }

Employer table
  Where: src/legitimateContracts.js, the `employer` array
  Notes: Add more employer types. Each entry uses:
           { text: "Blacksmith", rewardModifierPercent: 10, weight: 5 }

External Complication table
  Where: src/legitimateContracts.js, the `externalComplication` array
  Notes: Several entries need text filled in or refined. Each entry uses:
           { text: "some complication", rewardModifierPercent: 0 }

Dangerous Contract table
  Where: src/legitimateContracts.js, the `dangerousContract` array
  Notes: First entry uses rewardModifierPercent instead of baseRewardGp - check if intentional.

Weird Contract table
  Where: src/legitimateContracts.js, the `weirdContract` array
  Notes: Has one blank entry and one with baseRewardGp: 0. Fill these in or remove them.

Investigation Contract table
  Where: src/legitimateContracts.js, the `investigationContract` array
  Notes: Looks mostly complete. Review weights and add more entries if needed.

SEED CHANCES
------------

Legitimate seed chances
  Where: src/legitimateContracts.js, the `LEGITIMATE_SEEDS` array
  Notes: Marked as rough placeholder values. Review and adjust the `percent` values
         once the content in each seed type feels balanced. Values must total 100.


SETTINGS / CONSTANTS
---------------------

Seed chances should be reviewed once content is filled
  Where: src/legitimateContracts.js, LEGITIMATE_SEEDS array
  Notes: See above.
